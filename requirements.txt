aiogram>=3.20,<4.0
SQLAlchemy>=2.0
# Явно, не полагаясь на автоподтяжку SQLAlchemy: на Apple Silicon (macOS
# arm64) её platform-маркер не срабатывает (там платформа "arm64", а не
# "aarch64"), и greenlet, от которого зависит async-движок, не ставится сам.
greenlet>=3.0
aiosqlite>=0.19
asyncpg>=0.29
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
aiohttp_socks>=0.9
tzdata>=2024.1
openpyxl>=3.1
reportlab>=4.2
# Уменьшение загружаемых фотографий и аватаров (services/images.py). Без неё
# файл ложился на диск ровно таким, каким пришёл с телефона: снимок на 3,8 МБ
# ехал к читателю целиком ради картинки шириной в 350 точек.
Pillow>=10.3

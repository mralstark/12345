  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600;700&family=PT+Sans:wght@400;700&display=swap">
  <style>
    /* Токены сняты из webapp/styles.css без изменений. */
    body { margin: 0; }
    a { color: #d9b57b; } a:hover { color: #e8c68f; }
    .scr {
      width: 375px; height: 812px; overflow: hidden; position: relative;
      background: #141414; color: #ece6dc;
      font-family: "PT Sans", -apple-system, "Segoe UI", Roboto, sans-serif;
      font-size: 15px; line-height: 1.45;
      display: flex; flex-direction: column;
    }
    .pf { font-family: "Playfair Display", Georgia, serif; }
    .topbar {
      flex: none; background: #141414; border-bottom: 1px solid rgba(217,181,123,0.22);
      padding: 12px;
    }
    .topbar__title { font-family: "Playfair Display", Georgia, serif; font-size: 19px; line-height: 1.2; }
    .topbar__sub { font-size: 12px; color: #9c9082; }
    .view { flex: 1 1 auto; overflow: hidden; padding: 12px; display: flex; flex-direction: column; gap: 12px; }
    .card { background: #1e1b17; border-radius: 12px; padding: 12px; }
    .card__title { font-family: "Playfair Display", Georgia, serif; font-size: 16px; }
    .sub { font-size: 12px; color: #9c9082; }
    .row { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 12px 0; border-bottom: 1px solid rgba(217,181,123,0.22); }
    .row:last-child { border-bottom: none; }
    .row__side { text-align: right; white-space: nowrap; color: #9c9082; }
    .btn {
      display: inline-flex; align-items: center; justify-content: center; gap: 8px;
      min-height: 44px; padding: 12px 16px; border: none; border-radius: 10px;
      background: #d9b57b; color: #201a12; font-family: inherit; font-size: 14px; font-weight: 500;
    }
    .btn--ghost { background: #1e1b17; color: #ece6dc; box-shadow: inset 0 0 0 1px rgba(217,181,123,0.22); }
    .btn--sm { min-height: 32px; padding: 8px 12px; font-size: 13px; }
    .field { margin-bottom: 12px; }
    .field label { display: block; font-size: 12px; color: #9c9082; margin-bottom: 4px; }
    .input {
      width: 100%; padding: 8px 12px; border-radius: 9px; border: 1px solid rgba(217,181,123,0.22);
      background: #141414; color: #ece6dc; font-size: 16px; box-sizing: border-box;
    }
    .avatar { width: 36px; height: 36px; border-radius: 50%; background: rgba(217,181,123,0.22);
      display: flex; align-items: center; justify-content: center; font-weight: 700; flex: none; }
    .nav {
      flex: none; display: grid; grid-template-columns: repeat(5, minmax(0,1fr));
      background: #141414; border-top: 1px solid rgba(217,181,123,0.22);
    }
    .nav__i { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px;
      min-height: 56px; padding: 8px 4px; color: #9c9082; font-size: 11px; }
    .nav__i--on { color: #d9b57b; }
    .nav__i svg { width: 24px; height: 24px; }
    .tag { display: inline-block; padding: 4px 12px; border-radius: 999px; background: #141414;
      border: 1px solid rgba(217,181,123,0.22); font-size: 12px; color: #d9b57b; }
  </style>
